{
  "name": "stayinvallarta",
  "version": "0.1.0",
  "description": "Sitio web oficial de Stay in Vallarta — Astro 7 + Tailwind CSS 4 + TypeScript (estático, cero JS por defecto)",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "@fontsource-variable/fraunces": "^5.3.0",
    "@fontsource-variable/plus-jakarta-sans": "^5.3.0",
    "astro": "^7.1.6"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@tailwindcss/vite": "^4.3.3",
    "tailwindcss": "^4.3.3",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "comment_overrides": "sharp <0.35 arrastra CVEs de libvips. Astro lo declara como dependencia OPCIONAL con rango ^0.34 || ^0.35, así que 0.35.x es válido; npm se quedaba en 0.34.5 por el lockfile. Aquí nunca se ejecuta (no usamos astro:assets ni <Image>), pero dejarlo limpio evita que una alerta real se pierda entre el ruido.",
  "overrides": {
    "sharp": "^0.35.3"
  },
  "allowScripts": {
    "esbuild@0.28.1": true,
    "sharp@0.35.3": true
  }
}
